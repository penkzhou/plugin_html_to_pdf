import 'package:flutter_test/flutter_test.dart';

void main() {
  setUpAll(() async {
    TestWidgetsFlutterBinding.ensureInitialized();
  });
  group("HtmlToPdf tests", () {
    testWidgets('can render', (WidgetTester tester) async {});
  });
}
